<UserControl x:Class="VoidDays.Views.MainViewContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VoidDays.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <ContentControl Content="{Binding PreviousDayViewModel}"/>
        </Grid>
        <Grid Grid.Column="1">
            <ContentControl Content="{Binding CurrentListViewModel}"/>
        </Grid>
        <Grid Grid.Column="2">
            <ContentControl Content="{Binding CurrentDayViewModel}"/>
        </Grid>
    </Grid>
</UserControl>
